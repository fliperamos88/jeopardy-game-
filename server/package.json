{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "client": "npm install --prefix ../client && npm run build --prefix ../client && npm install",
    "dev": "concurrently \"npm run client\" \"npm start\""
  },
  "type": "commonjs",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.5",
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "express": "^4.18.2"
  }
}
